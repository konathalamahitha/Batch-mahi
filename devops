hi mahitha


NEW TELUGU 3.

GIT:

git add file1  === (workspace to staging)

git reset head file1 === (staging to workspace )

git commit -m "label" file1 --- singile file not mention the file name  ==(staging to local)

git reset --soft cid == (local to staging)


====================
delete file == rm -rf file1

local repo to workspace will move the files == but conditions apply 

staging data also move the work space 

git reset --mixed cid === (local to work space )

work space to local repo==new files not move 
old files will move the workspace to local repo

git commit -m "label" modified-file-name  (workspace to local) 

gir reset --hard cid ()  == (to delete commit and data) 


===================

git hub create one repo == clone the local repo 
repo name directory created == open the directory 
repo to local repo == clone



mahi1997K   ==== password git hub


git clone https://github.com/konathalamahitha/Batch-mahi.git

ls
cd Batch79
ls
touch add file1
git status
git commit -m "file1"
git status
git log 
git push
